name: CICD

on:
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 19.0.1
        uses: actions/setup-java@v2
        with:
          java-version: '19'
          distribution: 'adopt'